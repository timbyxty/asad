name: abc
on: [push]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3.3.0
    - run: npx embedme README.md
    - run: git add README.md
    - run: git commit -m "embedded code"
    - run: git push origin main

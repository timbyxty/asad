name: abc
on: [push]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
    - run: cat ${{ github.workspace }}"README.md"
    - run: npx embedme ${{ github.workspace }}"README.md"
